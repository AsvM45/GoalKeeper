<Window x:Class="ConfigUI.Views.TypingChallenge"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="GoalKeeper – Confirm Your Intention"
        Height="540" Width="640"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Topmost="True"
        Background="{DynamicResource MaterialDesign.Brush.Background}"
        TextElement.Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"
        TextElement.FontFamily="{md:MaterialDesignFont}">

    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <md:PackIcon Kind="KeyboardOutline" Width="28" Height="28"
                         Foreground="{DynamicResource MaterialDesign.Brush.Primary}"
                         Margin="0,0,12,0"/>
            <TextBlock Text="Prove Your Intent" FontSize="22" FontWeight="Bold"
                       VerticalAlignment="Center"/>
        </StackPanel>

        <TextBlock Grid.Row="1" TextWrapping="Wrap" Opacity="0.7" Margin="0,0,0,16"
                   Text="Type the paragraph below exactly as shown. One typo, one backspace – and it resets. This ensures you're committed, not impulsive."/>

        <!-- Target paragraph display -->
        <Border Grid.Row="2" CornerRadius="8" Padding="16" Margin="0,0,0,16"
                Background="{DynamicResource MaterialDesign.Brush.Card.Background}">
            <TextBlock x:Name="TargetText"
                       TextWrapping="Wrap"
                       FontFamily="Consolas, Courier New"
                       FontSize="13"
                       LineHeight="22"
                       Opacity="0.9"/>
        </Border>

        <!-- Input box -->
        <TextBox Grid.Row="3"
                 x:Name="InputBox"
                 AcceptsReturn="False"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 FontFamily="Consolas, Courier New"
                 FontSize="13"
                 md:HintAssist.Hint="Start typing here…"
                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                 PreviewKeyDown="InputBox_PreviewKeyDown"
                 TextChanged="InputBox_TextChanged"
                 Margin="0,0,0,16"/>

        <!-- Progress bar -->
        <ProgressBar Grid.Row="4"
                     x:Name="ProgressBar"
                     Height="6"
                     Minimum="0"
                     Value="{Binding ElementName=InputBox, Path=Text.Length}"
                     Margin="0,0,0,16"
                     Style="{StaticResource MaterialDesignLinearProgressBar}"/>

        <!-- Action buttons -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Click="CancelButton_Click"
                    Margin="0,0,12,0"/>
            <Button x:Name="ConfirmButton"
                    Content="Confirm"
                    IsEnabled="False"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Click="ConfirmButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
